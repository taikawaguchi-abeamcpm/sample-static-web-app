{
  "name": "feature-candidate-api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm run clean && tsc -p tsconfig.json",
    "clean": "rimraf dist",
    "start": "func start --typescript",
    "watch": "tsc -w --preserveWatchOutput"
  },
  "dependencies": {
    "@azure/functions": "3.5.1",
    "mssql": "^10.0.2"
  },
  "devDependencies": {
    "@types/mssql": "^9.1.5",
    "@types/node": "^20.11.30",
    "rimraf": "^6.0.1",
    "typescript": "^5.9.3"
  }
}
